# YOSSEUF OS v1.5.0 — Mobile Live Foundation

## هدف الإصدار
تحويل تطبيق Expo من نموذج واجهة ثابتة إلى عميل حقيقي متصل بنفس حساب Supabase وبيانات منصة الويب.

## ما تم تنفيذه
- تسجيل دخول فعلي بالبريد وكلمة المرور عبر Supabase Auth.
- حفظ الجلسة على الهاتف باستخدام AsyncStorage وتجديدها تلقائيًا.
- لوحة قيادة موبايل تعتمد على بيانات المشاريع والمهام والإشعارات الحية.
- قائمة مشاريع تعرض الحالة والأولوية والمرحلة والتقدم.
- مركز إشعارات مع دعم تحديد الإشعار كمقروء.
- تحديث يدوي للبيانات ومعالجة واضحة لأخطاء المزامنة.
- تسجيل خروج آمن.
- فصل الشاشات والخدمات والأنواع إلى وحدات مستقلة قابلة للتوسع.

## قاعدة البيانات
لا يحتاج الإصدار إلى Migration جديدة. يستخدم جداول Supabase الحالية وسياسات RLS الموجودة.

## ملاحظات التشغيل
انسخ `mobile/.env.example` إلى `mobile/.env` وأدخل مفاتيح Supabase العامة، ثم نفّذ `npm install` و`npm run start` داخل مجلد `mobile`.
