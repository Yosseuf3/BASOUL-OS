# Contributing

During the v1.0 code freeze, changes are limited to blocker fixes, security fixes, data integrity fixes, and documentation corrections.

Before submitting a change:

```bash
npm run quality
```

Update CHANGELOG and the relevant test checklist with every release-affecting change.
